package com.example.fds.model;

/**
 * Enum representing the types of rules supported in the system.
 * <ul>
 *   <li>{@code GEO_DISTANCE}: Rule based on geographical distance.</li>
 *   <li>{@code COUNT_THRESHOLD}: Rule based on a count threshold.</li>
 * </ul>
 */
public enum RuleType {
    GEO_DISTANCE, COUNT_THRESHOLD
};
